'use strict';

const productService = require('../services/product');

module.exports = {

  list: async (ctx) => {

  },

  /**
   * Search product
   *
   * @api POST /product/search (not conflict with /product/:id)
   *
   * @param ctx
   * @return {Promise<void>}
   */
  search: async (ctx) => {

  }

};
